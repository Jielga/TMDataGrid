---
"@jielga/tmdatagrid": patch
---

`TMDataGrid.Menu.Columns` and `TMDataGrid.ColumnsPanel` show their search box from six hideable columns.
`searchable` is `TMDataGridColumnSearchable` (`boolean | "auto"`), default `"auto"`; `true` keeps the box on a shorter list.
