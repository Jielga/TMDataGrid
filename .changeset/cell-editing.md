---
"@jielga/tmdatagrid": minor
---

Cell editing (`editMode: "cell"`): one TanStack Form per editing row, built-in editors per column type, `meta.validate` / `rowValidators` with Standard Schema support, `meta.renderEditor`, keyboard entry and `onEditCommit`. Adds `@tanstack/react-form` as a peer dependency.
