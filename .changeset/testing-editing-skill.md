---
"@jielga/tmdatagrid": patch
---

Testing docs for editing flows: where an added row's temporary id goes at ✓ and at Save, how a test finds the row afterwards, and recipes for validation, cell and row edits, deletions and the draft store, with the matching page-object methods. A new `testing-editing` skill carries the same.
