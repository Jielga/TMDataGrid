---
"@jielga/tmdatagrid": minor
---

`onCellClick`, `onCellDoubleClick` and `onCellContextMenu` on `TMDataGrid.Table` - `{ cell, row, column, event }`, composing with selection, editing and the context menu rather than replacing them.
