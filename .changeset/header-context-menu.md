---
"@jielga/tmdatagrid": patch
---

Right-clicking a column header opens the same menu as the ⋮ button, at the
pointer. Headers without a menu — the checkbox and details lanes — keep the
browser's own.
