---
"@jielga/tmdatagrid": minor
---

New: `edit.getRowValues(rowId)` and `edit.getRows()` - rows as shown, drafts overlaid, deletion marks and entry rows flagged rather than filtered. New type `TMDataGridEditRowSnapshot`.
